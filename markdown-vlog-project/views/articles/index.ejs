<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
   <link rel="stylesheet" href="../../public/style.css">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Blog</title>
</head>
<body>
  <div class="container">

    <h1 class="mb-4" style="color : white ; text-align: center; ">Blog Articles</h1>
    <a href="/articles/new" class="btn btn-success">New Article</a>
   
    <% articles.forEach(article => { %>
      <div class="card mt-4 divcolor">
        <div class="card-body articles">
        <h4 class="card-title articletitle"><%= article.title %></h4>
          <div class="card-subtitle mb-2 datediv">
            <%= article.createdAt.toLocaleDateString() %>
          </div>
          <div class="card-text mb-2 descriptiondiv"><%= article.description %></div>
          <a href="articles/<%= article.slug %>" class="btn btn-primary">Read More</a>

          <a href="articles/edit/<%= article.id %>" class="btn btn-info">Edit</a> <!--edit for article.id-->

          <form action="/articles/<%= article.id %>?_method=DELETE" 
            method="POST" class="d-inline">        <!--(d-inline) for lie in same line-->

            <button type="submit" class="btn btn-danger">Delete</button>
          </form>
        </div>
      </div>
    <% }) %>
  </div>
</body>
</html>